# SCalculator
A very simple calculator in C.

This calculator provides the simple operations like <code>+, -, *, /</code>
It does not have more complex calculations implemented yet.


Run it on macOS:

<code>user@mac ~ % clang main.c -o main</code>

<code>user@mac ~ % ./main</code>
